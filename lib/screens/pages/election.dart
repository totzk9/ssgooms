import 'package:flutter/material.dart';

class ElectionView extends StatelessWidget {
  const ElectionView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
